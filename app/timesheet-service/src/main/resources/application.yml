spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/timesheetdb
    username: timesheet_user
    password: rootpwd
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081


